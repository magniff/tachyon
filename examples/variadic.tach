extern fn printf(format: *u8, ...);

fn main() -> u8 {
    let integer = 1;
    let float = 2.0;
    printf("Just a humble int %d\n".0, integer);
    printf("A fancy float %f\n".0, float);
    printf("These two together, friendship, hey! %d and %f\n".0, integer, float);
    0
}